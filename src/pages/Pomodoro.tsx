import { Pomodoro as PomodoroComponent } from '@/components/Pomodoro';

const PomodoroPage = () => {
  return (
    <div className="container mx-auto">
      <PomodoroComponent />
    </div>
  );
};

export default PomodoroPage;